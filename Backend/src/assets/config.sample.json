{
    "database": {
        "host": "host",
        "port": 3306,
        "user": "username",
        "password": "password"
    },
    "forces": {
        "psp": {
            "name": "Polícia de Segurança Pública",
            "acronym": "PSP",
            "patrols": ["psp", "gnr"],
            "database": "database",
            "isPromotion": "($oldpatent !== $newpatent) || ($oldstatus === 1 && $newstatus === 4) || ($oldstatus === 3 && $newstatus === 4)",
            "patents": {
                "recruit": 0,
                "default": 1
            },
            "inactivity_justification_type": 1,
            "inactive_status": 1,
            "maximum_non_working_days": 5,
            "minumum_week_minutes": 300
        },

        "gnr": {
            "name": "Guarda Nacional Republicana",
            "acronym": "GNR",
            "patrols": ["psp", "gnr"],
            "database": "database",
            "isPromotion": "($oldpatent !== $newpatent) || ($oldstatus === 1 && $newstatus === 4) || ($oldstatus === 3 && $newstatus === 4)",
            "patents": {
                "recruit": 0,
                "default": 1
            },
            "inactivity_justification_type": 1,
            "inactive_status": 1,
            "maximum_non_working_days": 5,
            "minumum_week_minutes": 300
        }
    }
}